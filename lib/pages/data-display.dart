import 'package:flutter/material.dart';

class DataDisplayPage extends StatelessWidget {
  final arguments;

  DataDisplayPage({this.arguments});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DataDisplayPage'),
    );
  }
}
