import 'package:flutter/material.dart';

class DataEntryPage extends StatelessWidget {
  final arguments;

  DataEntryPage({this.arguments});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DataEntryPage'),
    );
  }
}
